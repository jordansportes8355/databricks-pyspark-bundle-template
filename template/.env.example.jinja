# Copy to .env and fill in for local development — never commit .env.

# Databricks Connect / CLI auth (alternatively use a ~/.databrickscfg profile)
# DATABRICKS_CONFIG_PROFILE=DEFAULT
# DATABRICKS_HOST={{ databricks_host }}
# DATABRICKS_CLUSTER_ID=   # leave empty to use serverless compute

# Local fallbacks for secrets read via {{ package_name }}.secrets.get_secret(..., env_var=...)
# MY_API_KEY=
